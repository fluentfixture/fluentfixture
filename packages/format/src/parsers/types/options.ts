/**
 * `Options` of compiling process.
 * @interface
 */
export interface Options {
  /**
   * If set to false, will throw when an error occurred in process,
   * otherwise ignore errors and use default values. Default is true.
   */
  ignoreErrors?: boolean;
}
